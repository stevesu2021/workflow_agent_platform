“智能体”页面，在构建workflow智能体画布的“规划”页面，对于“开始”节点，如果有文件上传，不仅仅要在fileNames里面保存那些文件名称，还需要在fileUrls字段里，保存上传的文件在minIO的完整地址，供后续其他节点引用。

“开始”节点需要有个标识名，比如“start_node”,并显示在“开始”节点的编辑面板，不可修改。

另外在编辑一个现有的workflow智能体的时候，在点击“保存”按钮的时候，不要让用户再次填写智能体名称，继续保存在原有的智能体里就好。再提供一个“另存为”按钮，点击“另存为”可以保存为新的智能体。 
