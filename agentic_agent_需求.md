 “智能体”页面，构建Agentic智能体的页面，需要在生成需求文档的同时，生成一份资源清单，用于记录这个智能体用到的所有资
  源信息，包括不限于LLM，PaddleOCR，知识库及地址等。然后在“生成代码”环节生成一份.env文件。需要在右侧添加一个显示的l
  oop次数，默认写0次。还需要对于IO配置中，对Input有要求的，在右侧提供一个default input的配置入口。
然后要求点击“生成代码”的时候，生成完代码，还需要自动构建venv环境，并就自动调用“运行代码”，input用配置好的默认内容，运行有问题的时候，将问题直接给到“生成代码”模块，自动修改代码。这个过程就是loop中的一次，给loop计数加1。如果loop次数到达10次问题还存在，从循环里退出。生成代码和运行代码环节，中间的各种数据和日志等编号过程，在右侧用一个抽屉，把内容写到抽屉里。

另外生成的代码的版本要求：
langchain >= 1.0
langgraph >= 1.0
