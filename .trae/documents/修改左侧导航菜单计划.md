我已完成对前端代码的分析，以下是修改导航菜单的计划：

**目标文件**: `frontend/src/layouts/MainLayout.tsx`

**修改内容**:
1.  **添加图标引用**:
    *   引入 `ToolOutlined` (用于工具管理)
    *   引入 `ApiOutlined` (用于MCP管理)
    *   使用现有的 `TeamOutlined` 或引入 `ClusterOutlined` (用于Agentic智能体)
2.  **重构菜单配置 (`items` 数组)**:
    *   **重命名**: 将 "概览" 改为 "**概述**"。
    *   **重命名**: 将 "系统设置" 改为 "**系统资源**"。
    *   **新增**: 添加 "Agentic智能体"、"MCP管理"、"工具管理" 三个菜单项。
    *   **排序**: 按照您要求的顺序重新排列菜单项。

**调整后的菜单结构**:
1.  **概述** (`/`) - 图标: `PieChartOutlined`
2.  **智能体管理** (`/agents`) - 图标: `RobotOutlined`
3.  **Agentic智能体** (`/agentic-agents`) - 图标: `TeamOutlined` (新增)
4.  **工作流编排** (`/workflow`) - 图标: `NodeIndexOutlined`
5.  **知识库** (`/knowledge`) - 图标: `BookOutlined`
6.  **MCP管理** (`/mcp`) - 图标: `ApiOutlined` (新增)
7.  **工具管理** (`/tools`) - 图标: `ToolOutlined` (新增)
8.  **系统资源** (`/settings`) - 图标: `SettingOutlined`

**确认**:
是否确认按照上述计划修改左侧导航菜单？修改后您需要刷新前端页面查看效果。
